yaml + database -> SerializedObject

SerializedObject = any serializable field value 
float,int,string...
array
map
shared_ptr<Object>

can contain no value

can be merged with other SerializedObject

Any object can be serialized into SerializedObject
Any object can be deserialized from SerializedObject

Any object values can be overriden from SerializedObject

Can be traversed

Can be serialized into YAML
Can be deserialized from YAML + database

Faster than YAML?

Prefab = asset + SerializedObject -> (asset to SerializedObject + merge) -> Instantiate from merged SerializedObject

Inspector = edit Serialized -> ???

change asset uid to something like $meshes\playerMesh.glb$Box